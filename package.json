{
  "name": "roiheimen",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "cd pkg/web; yarn build",
    "setup": "cd pkg/server; yarn setup",
    "start:web": "cd pkg/web; yarn start",
    "start:server": "cd pkg/server; yarn start",
    "start": "run-p start:server start:web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.1.2"
  }
}
